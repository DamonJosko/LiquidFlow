@model IEnumerable<LiquidFlowLogin.Models.CountyState>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CountyName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CountyName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CountyStateID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CountyStateID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CountyStateID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
